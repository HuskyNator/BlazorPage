@inject NavigationManager NavigationManager

<button @onclick="ToonMenu">Menu</button>

<button @onclick="@(e=>verwijs("/"))">Hoofd<br>Scherm</button>
<button @onclick="@(e=>verwijs("/Plan"))">Plan</button>
<button @onclick="@(e=>verwijs("https://github.com/HuskyNator/BlazorPage"))">Github</button>
<button @onclick="@(e=>verwijs("#"))">#</button>

@code {
    private bool zichtbaar = true;

    //private string MenuBalk => zichtbaar ? "MenuBalk" : null;

    private void ToonMenu()
    {
        zichtbaar = !zichtbaar;
    }

    private void verwijs(String locatie){
        NavigationManager.NavigateTo(locatie);
    }
}
